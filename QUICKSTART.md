# 快速入门指南

## 📦 安装

### Linux / macOS

```bash
# 1. 运行安装脚本（推荐）
./install.sh

# 或者手动安装
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Windows

```cmd
REM 1. 运行安装脚本（推荐）
install.bat

REM 或者手动安装
python -m venv venv
venv\Scripts\activate.bat
pip install -r requirements.txt
```

## ⚙️ 配置

```bash
# 1. 复制环境变量模板
cp .env.example .env

# 2. 编辑 .env 文件，添加至少一个翻译 API key
nano .env  # 或使用你喜欢的编辑器

# 必需配置（至少一个）:
#   OPENAI_API_KEY=sk-xxx...    # OpenAI API (推荐)
#   DEEPL_API_KEY=xxx...        # DeepL API (可选)
```

## 🚀 快速开始

### 方式 1: 使用启动脚本（推荐）

```bash
# Linux/Mac
./start.sh

# Windows
start.bat
```

启动脚本提供交互式菜单，包含所有常用操作。

### 方式 2: 使用命令行

```bash
# 激活虚拟环境
source venv/bin/activate  # Linux/Mac
# 或 venv\Scripts\activate.bat  # Windows

# 运行完整流程（推荐首次使用）
python main.py pipeline

# 查看新闻列表
python main.py show

# 查看新闻详情（从列表中复制 ID）
python main.py detail <news_id>
```

### 方式 3: 使用 Makefile（Linux/Mac）

```bash
make install   # 安装
make pipeline  # 运行完整流程
make show      # 显示新闻
make stats     # 查看统计
```

## 📖 常用命令

### 抓取新闻

```bash
# 抓取所有新闻源
python main.py fetch

# 抓取指定新闻源
python main.py fetch -s reuters
python main.py fetch -s hackernews

# 抓取并立即翻译验证
python main.py fetch -t -v
```

### 查看新闻

```bash
# 显示最新 20 篇新闻
python main.py show

# 显示 50 篇新闻
python main.py show -l 50

# 筛选高可信度新闻（评分 >= 0.8）
python main.py show -m 0.8

# 显示最近 7 天的科技新闻
python main.py show -c 科技 -d 7

# 显示特定来源的新闻
python main.py show -s Reuters
```

### 翻译和验证

```bash
# 翻译未翻译的新闻
python main.py translate

# 翻译 50 篇
python main.py translate -l 50

# 验证未验证的新闻
python main.py validate

# 验证 50 篇
python main.py validate -l 50
```

### 统计和清理

```bash
# 查看统计信息
python main.py stats

# 清理 30 天前的旧新闻
python main.py clean

# 清理 7 天前的旧新闻
python main.py clean -d 7
```

### 完整流程

```bash
# 一键运行：抓取 → 翻译 → 验证
python main.py pipeline

# 指定新闻源
python main.py pipeline -s reuters -s hackernews
```

## 🎨 显示效果预览

### 新闻列表
```
┏━━━┳━━━━━━━━━━━━━━━━━━━━━━━━━━━┳━━━━━━━━━━━━┳━━━━━━━━┳━━━━━━┳━━━━━━━━━━━━┓
┃ # ┃ 标题                      ┃ 来源       ┃ 可信度 ┃ 时间       ┃
┡━━━╇━━━━━━━━━━━━━━━━━━━━━━━━━━━╇━━━━━━━━━━━━╇━━━━━━━━╇━━━━━━╇━━━━━━━━━━━━┩
│ 1 │ Breaking News: Major...   │ Reuters    │ ★★★★★ │ 中立 │ 02-04 10:30│
│ 2 │ Tech Innovation in...     │ Hacker News│ ★★★☆☆ │ 中立 │ 02-04 09:15│
└───┴───────────────────────────┴────────────┴────────┴──────┴────────────┘
```

### 新闻详情
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Breaking News: Major Event
重大突发新闻：重要事件

🌐 来源: Reuters
🔗 链接: https://...
🕒 时间: 2026-02-04 10:30:00
📁 分类: 国际

📊 验证信息
  可信度评分  ★★★★★ 0.95/1.0
  交叉引用    8 个来源

✅ 高可信度  ✅ 多源证实

📰 内容
┌──────────────────────────────────────────────────┐
│ [双语对照显示]                                    │
│ 原文：...                                         │
│ 中文：...                                         │
└──────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## ❓ 常见问题

### Q: 翻译功能不工作？
A: 请确保在 `.env` 文件中配置了有效的 `OPENAI_API_KEY` 或 `DEEPL_API_KEY`。

### Q: 如何添加更多新闻源？
A: 在 `src/fetchers/` 目录下创建新的抓取器类，继承 `BaseFetcher`。参考 `reuters.py` 的实现。

### Q: 验证功能需要什么？
A: 可信度评估和事实核查不需要额外配置。

### Q: 如何设置自动定时抓取？
A: 使用 `./deploy.sh` 脚本，它会帮助你设置 crontab 定时任务。

### Q: 数据存储在哪里？
A: SQLite 数据库存储在 `data/news.db`，可以使用任何 SQLite 工具查看。

## 🔧 开发

### 安装开发依赖

```bash
pip install -r requirements-dev.txt
```

### 运行测试

```bash
# 运行所有测试
pytest

# 查看覆盖率
pytest --cov=src --cov-report=html

# 使用开发脚本
./dev.sh
```

### 代码质量

```bash
# 格式化代码
black .

# Lint 检查
ruff check .

# 类型检查
mypy src/

# 或使用 Makefile
make check
```

## 📚 更多资源

- **完整文档**: `README.md`
- **AI 代理指南**: `AGENTS.md`
- **项目配置**: `pyproject.toml`

## 💡 提示

1. 首次使用建议运行 `python main.py pipeline` 体验完整流程
2. 使用 `python main.py show -m 0.8` 查看高质量新闻
3. 定期运行 `python main.py clean` 清理旧数据
4. 查看 `python main.py --help` 了解所有命令
5. 使用 `./start.sh` 获得最佳交互体验

## 🆘 获取帮助

```bash
# 查看所有命令
python main.py --help

# 查看特定命令帮助
python main.py fetch --help
python main.py show --help
```

祝使用愉快！🎉
